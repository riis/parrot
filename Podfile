# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'ParrotDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'GroundSdk', '1.0.0'
  pod 'ArsdkEngine', '1.0.0'
  pod 'SdkCore', '1.0.0' 
  # Pods for ParrotDemo

  target 'ParrotDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ParrotDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
